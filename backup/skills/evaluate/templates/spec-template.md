# Design Spec: {Subject}

**Date**: {date}
**Author**: /product-dev spec
**Product type**: {UI App | CLI | API / Service | Library | Infrastructure}
**Brief**: {link to product/brief.md if exists, or "inferred from context"}
**Scope**: {component | feature | view | module | product}

---

## Overview

{2-3 sentences. What this spec covers and what job it serves.}

## Information Architecture

{Describe the content/structure hierarchy. Adapt to product type.}

### Content Map
```
{For UI: view hierarchy}
{For CLI: command tree}
{For API: resource hierarchy}
{For Library: module/export tree}

{View/Command/Resource}
├── {Primary area/subcommand/endpoint}
│   ├── {Element} — {what data, from where}
│   └── {Element} — {what data, from where}
├── {Secondary area}
│   └── {Element}
└── {Tertiary/support}
    └── {Element}
```

## User Flows

### Flow 1: {Job name}

**Trigger**: {What initiates this flow}
**Success state**: {What the user sees/gets when it works}

| Step | User sees/gets | User does | System responds |
|------|---------------|-----------|-----------------|
| 1 | {content/output} | {action} | {result} |
| 2 | {content/output} | {action} | {result} |

**Error path**: {What happens when it fails at step N}
**Edge cases**: {Unusual but valid scenarios}

### Flow 2: {Job name}
{Same structure}

## State Definitions

### {Surface Name}

{Use the state table appropriate for product type:}

{UI:}
| State | Condition | Content | Actions available |
|-------|-----------|---------|-------------------|
| Empty | {when} | {what shows} | {what user can do} |
| Loading | {when} | {skeleton/spinner} | {wait} |
| Error | {when} | {message + recovery} | {retry/report} |
| Active | {when} | {full content} | {all interactions} |

{CLI:}
| State | Condition | Output | Exit code |
|-------|-----------|--------|-----------|
| Success | {when} | {stdout content} | 0 |
| No results | {when} | {stderr message} | 0 |
| User error | {when} | {stderr + help hint} | 1 |
| System error | {when} | {stderr diagnostic} | 2+ |

{API:}
| State | Condition | Response body | Status code |
|-------|-----------|--------------|-------------|
| Success | {when} | {shape} | 200/201 |
| Validation error | {when} | {error shape} | 400 |
| Not found | {when} | {message} | 404 |

## Surface Spec

### New Surfaces

#### {Name} — {view | command | endpoint | export}
- **Purpose**: {what it does}
- **Used in / accessed via**: {where/how}
- **Inputs**: {props, args, params, request body}
- **States**: {list}
- **Interactions / behaviors**: {what happens}
- **Data source**: {store, API, filesystem, etc.}

### Modified Surfaces

#### {Name}
- **Current**: {what it does now}
- **Change**: {what changes}
- **Reason**: {why}

### Removed Surfaces
- {Name} — {why removed}

## Data Requirements

| Data point | Source | Update frequency | Fallback |
|-----------|--------|-----------------|----------|
| {data} | {store/API/filesystem} | {real-time/on-load/manual} | {what to show/return if unavailable} |

## Design Notes

{Direction, not pixel-perfect specs. Adapt to product type.}

{For UI:}
- **Layout**: {grid, sidebar+main, single column, etc.}
- **Density**: {compact/comfortable/spacious}
- **Key visual elements**: {badges, progress bars, status dots, etc.}
- **Color coding**: {what colors mean}

{For CLI:}
- **Output format**: {tables, JSON, plain text, colored}
- **Progress indication**: {spinner, progress bar, streaming}
- **Verbosity levels**: {default, --verbose, --quiet}

{For API:}
- **Response format**: {JSON structure, pagination style, envelope vs flat}
- **Error format**: {consistent error object shape}
- **Versioning**: {URL path, header, none}

## Implementation Notes

### Files to create
- {path} — {purpose}

### Files to modify
- {path} — {what changes}

### Data model changes
- {type/schema change}

### Migration steps
{If changing existing behavior, how to transition}

## Test Plan

### Manual verification
1. {Check this works}
2. {Check this state/response is correct}

### Automated tests
- {Test case}: {what it verifies}

---

*Generated by `/product-dev spec`. Implement with `/start-task --plan product/spec.md`.*
